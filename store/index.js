import axios from 'axios';
export const actions = {
    async nuxtServerInit({ state, dispatch }) {
    },
    async clientHook({ state, dispatch, commit }, context) {
        console.log("god bless my music god bless")
    }
}
